generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model CrateSrc {
  id   String @id @default(uuid())
  path String
  type String
}

model FilesDirectory {
  id   String @id @default(uuid())
  path String @unique
}
